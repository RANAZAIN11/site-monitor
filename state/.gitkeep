# This folder holds last-run.json — the daily issue snapshot the monitor
# commits after each run so the next run can diff against it. Do not delete;
# deleting it makes the next run treat every issue as "new".
